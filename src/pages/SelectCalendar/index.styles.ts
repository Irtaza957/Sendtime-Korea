import styled from '@emotion/styled';

export const SelectCalendarWrapper = styled.div`
  display: flex;
  flex-direction: column;
  height: 400px;
`;

export const Skip = styled.a`
  font-size: 15px;
  text-decoration: underline;
  color: var(--gray-600);
  text-align: center;
`;
